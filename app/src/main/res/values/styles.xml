<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyCheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/color_242424</item>
        <item name="colorControlActivated">@color/appbar_bkg</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@color/color_242424</item>
    </style>

    <style name="ToolbarTitleAppearance">
        <item name="android:textColor">@color/color_242424</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="ToolbarSubtitleAppearance">
        <item name="android:textColor">@color/color_a0a0a0</item>
        <item name="android:textSize">10sp</item>
    </style>

</resources>
